# payment-stream

Auto-generated TypeScript bindings for the `payment-stream` Soroban smart contract.

> **Note:** This directory is generated by the Soroban CLI. Do not edit files inside `src/` by hand — they will be overwritten on the next regeneration.

## Regenerating bindings

Run the following command from the repo root, substituting the values for your target network:

```bash
stellar contract bindings ts \
  --rpc-url https://soroban-testnet.stellar.org \
  --network-passphrase "Test SDF Network ; September 2015" \
  --contract-id <PAYMENT_STREAM_CONTRACT_ID> \
  --output-dir packages/sdk/src/generated/payment-stream
```

**Where to get the values:**

| Value | Source |
|-------|--------|
| `--contract-id` | `NEXT_PUBLIC_PAYMENT_STREAM_CONTRACT_ID` in `apps/web/.env` (see `apps/web/.env.example`) |
| `--rpc-url` | `NEXT_PUBLIC_SOROBAN_RPC_URL` in `apps/web/.env` — testnet default: `https://soroban-testnet.stellar.org` |
| `--network-passphrase` | Testnet: `"Test SDF Network ; September 2015"` / Mainnet: `"Public Global Stellar Network ; September 2015"` |

Contract IDs for deployed instances are also recorded in `deployments/testnet.json` (if present).

## Usage

This package is consumed internally by `@fundable/sdk`. You do not need to import it directly. See the [SDK README](../../../README.md) and the top-level [README](../../../../../README.md) for usage examples.

```ts
import { PaymentStreamClient } from '@fundable/sdk';

const client = new PaymentStreamClient({
  contractId: process.env.NEXT_PUBLIC_PAYMENT_STREAM_CONTRACT_ID!,
  networkPassphrase: 'Test SDF Network ; September 2015',
  rpcUrl: 'https://soroban-testnet.stellar.org',
});
```
